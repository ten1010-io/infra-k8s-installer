apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: prom-infra-alerter
  name: prom-infra-alerter
  namespace: {{ include "efkNamespace" . }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    k8s-app: prom-infra-alerter
