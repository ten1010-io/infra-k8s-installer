apiVersion: v1
kind: Service
metadata:
  name: "aipub-sso-gateway"
  labels:
    {{- include "aipub-sso-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aipub-sso-gateway.selectorLabels" . | nindent 4 }}
