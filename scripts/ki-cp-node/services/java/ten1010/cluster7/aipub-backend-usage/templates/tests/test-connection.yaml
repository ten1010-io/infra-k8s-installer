apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aipub-backend-usage.fullname" . }}-test-connection"
  labels:
    {{- include "aipub-backend-usage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aipub-backend-usage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
