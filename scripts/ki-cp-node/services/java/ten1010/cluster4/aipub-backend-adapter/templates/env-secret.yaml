apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-envs
  labels:
    {{- include "aipub-backend-adapter.labels" . | nindent 4 }}
stringData:
  APP_DOMAIN_AUTH_BASIC_AUTH_PASSWORD: {{ .Values.applicationYaml.app.domain.auth.basicAuthPassword | quote }}
