apiVersion: v1
kind: Service
metadata:
  name: "aipub-frontend"
  labels:
    {{- include "aipub-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aipub-frontend.selectorLabels" . | nindent 4 }}
