apiVersion: v1
kind: Service
metadata:
  name: "aipub-backend-gateway"
  labels:
    {{- include "aipub-backend-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aipub-backend-gateway.selectorLabels" . | nindent 4 }}
