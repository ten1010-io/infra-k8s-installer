- hosts: k8s_node
  gather_facts: false
  any_errors_fatal: true
  tasks:
    - name: Execute monitoring uninstall.sh
      shell: "sudo {{ ki_env_scripts_path }}/ki-cp-node/services/monitoring/installer/uninstall.sh all -y"
      when: inventory_hostname == k8s_cp_nodes | first
