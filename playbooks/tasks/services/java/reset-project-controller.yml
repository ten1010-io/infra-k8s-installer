- hosts: k8s_node
  gather_facts: false
  any_errors_fatal: true
  tasks:
    - name: Delete project-controller namespace
      shell: "sudo kubectl delete namespace project-controller --ignore-not-found"
      when: inventory_hostname == k8s_cp_nodes | first
